pipeline {
    agent any

    stages {
        stage('Backend Unit Test') {
            steps {
                echo 'Unit test Starting'
                go test -v ./...
            }
        }
    }
}
