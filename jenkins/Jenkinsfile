pipeline {
    agent any

    stages {
        stage('Backend Unit Test') {
            agent {
                docker {
                    image 'snakee/golang-junite:1.21'
                    reuseNode true
                }
            }
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
